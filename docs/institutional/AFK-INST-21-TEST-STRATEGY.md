---
Document ID: AFK-INST-21
Title: Test Strategy
Purpose: Test levels, coverage targets, environments, CI gates and acceptance criteria for Afrikoba Global.
Owner: QA Lead
Status: DRAFT
Version: 1.21
Effective Date: 2026-09-07
Last Review Date: 2026-09-11
Related Systems/Modules: All; scripts/test-*.js; .github/workflows/ci.yml
Related Regulatory Requirements: AFK-INST-13 evidence (test suites prove controls)
Approval Authority: QA Lead / CAB
---

# Test Strategy (AFK-INST-21)

## 1. Test levels

| Level | Scope | Where |
|-------|-------|-------|
| Unit | Services/utils (engine, ledger math, fee/split/WHT) | Node tests |
| Integration/Regression | End-to-end API + DB against a seeded Postgres | `scripts/test-*.js` (61 suites) + CI |
| Frontend build | Web dashboard Vite build; Flutter analyze/test/build | CI |
| Load/Security | k6 load + security scripts (`.github/workflows/k6.yml`, manual vs staging) | staging |
| Continuous monitoring | `uptime.yml` health probes sitting on `/health` + landing + `/api/v1/docs.json` | scheduled |
| UAT/Regression on release | Stable behaviour proof for money math | CI on `main` |

## 2. Coverage targets

- Money correctness: every transfer/deposit/withdrawal/split/disbursement asserts fee math,
  ledger DR=CR, wallet deltas, idempotency (e.g. test-ledger-integrity, test-all, multisplit).
- Security/RBAC: 401/403 on protected ops across suites (device binding, chart-of-accounts, tracing, AI).
- Ops/observability: chart numbering (21 checks), tracing spans (19), partitions, outbox, trace trees.
- Coverage by domain mapped in the suites below.

## 3. CI gates (`.github/workflows/ci.yml`)

- Backend: `npm run db:setup` → `npm run seed` → `npm run db:migrate` → start server
  (RATE_LIMIT_DISABLED=true DISABLE_CRON=true on :3000) → run suite list → fail on exit≠0.
- Dashboard: Vite build. Mobile: flutter analyze + test + build.
- Server log uploaded on failure for triage.

## 4. Backend regression suite list (wired in CI)

test-all, test-services, test-vicoba, test-rosca, test-p2p, test-events-stage4,
test-events-stage5, test-vicoba-inbox, test-features, test-four-eyes, test-experiments,
test-kyc, test-lending-gates, test-kilimo-seasons, test-disputes, test-support, test-currency, test-qr, test-insurance, test-cards, test-saccos-foundation, test-saccos-shares, test-saccos-savings, test-saccos-credit,
test-saccos-governance, test-saccos-accounting, test-saccos-investments, test-saccos-dividends, test-saccos-funds, test-saccos-exits, test-saccos-reports, test-saccos-installments, test-saccos-arrears, test-saccos-meetings, test-saccos-savings-interest, test-saccos-backing-welfare, test-saccos-loan-mgmt,
test-merchant-payouts,
test-outbox, test-vault, test-caching, test-partitions, test-ussd, test-multi-country,
test-ledger-integrity, test-field-partners, test-device-binding, test-chart-of-accounts,
test-tracing, test-ai-insights, test-sar-filing, test-openapi, test-procurement, test-tcra-ussd,
test-recurrence, test-payment-requests, test-merchant-invoices, test-marketplace,
test-family-guardian, test-projection-cache, test-restore-verify.

## 5. Known flakiness & rerun policy

- `test-kilimo-seasons` exact-count flake on a shared dev DB — intentionally not weakened.
- `test-kyc` transient CI flake — rerun green. CI job re-runs on flake per infra policy.

## 6. Acceptance (Definition of Done)

Per roadmap §6: backend endpoint + validation/RBAC; idempotent + transactional; SMS where
expected; dashboard UI wired; test proving money math; audit-log entry for money/privileged actions.

## Change History

| Version | Date | Author | Reason | Approval |
|---------|------|--------|--------|----------|
| 0.1 | 2026-09-07 | AI code review | Baseline from CI workflow + suite inventory + standards audit | QA Lead (pending) |
| 0.2 | 2026-09-07 | AI code review | Added test-procurement (C1 suppliers reconciliation / procurement lifecycle / financing) → 31 suites | QA Lead (pending) |
| 0.3 | 2026-09-07 | AI code review | Added test-tcra-ussd (097 shortcode registry / admin lifecycle / RBAC) → 32 suites | QA Lead (pending) |
| 0.4 | 2026-09-08 | AI code review | Added test-restore-verify (AFK-INST-18 DR: pg_dump→restore→integrity, 29 checks) → 33 suites | QA Lead (pending) |
| 0.5 | 2026-09-08 | AI code review | Added test-recurrence (STANDING_INSTRUCTION recurring transfers: SI-* idempotent journal transfer, recurrence_executions, RBAC, disabled-skip) → 34 suites | QA Lead (pending) |
| 0.6 | 2026-09-08 | AI code review | Added test-payment-requests (migration 098 request-to-pay: PRQ-* lifecycle, canonical transfer settlement TR-*, idempotent re-pay, cancel, expiry, RBAC, audit) → 35 suites | QA Lead (pending) |
| 0.7 | 2026-09-08 | AI code review | Added test-merchant-invoices (migration 099 merchant invoices: INV-* lifecycle, canonical merchant-payment settlement MERCH-*, idempotent re-pay, overpay/cancel/expiry guards, RBAC) → 36 suites | QA Lead (pending) |
| 0.8 | 2026-09-09 | AI code review | Added test-marketplace (marketplace money path: purchase→MARKETPLACE_ESCROW hold, evidence→settle, cancel→refund, dispute freeze + ADMIN BUYER_REFUND/SPLIT rulings with balanced journals, RBAC) → 37 suites | QA Lead (pending) |
| 0.9 | 2026-09-09 | AI code review | Added test-family-guardian (family wallet guardian controls: can_spend/spending_limit enforcement, OWNER-only invite/remove, INVITED→ACTIVE lifecycle, contribute/spend/transfer ledger + RBAC) → 38 suites | QA Lead (pending) |
| 1.0 | 2026-09-09 | AI code review | Added test-projection-cache (projection-cache audit: `wallet_balance` == `wallet_ledger` trail net across seed/transfer/PRQ/SI paths + drift-detection self-test) → 39 suites; suite list corrected to include test-marketplace/test-family-guardian; k6 + uptime CI workflows documented | QA Lead (pending) |
| 1.1 | 2026-09-09 | AI code review | Added test-support (migration 100 `resolved_at` backfill + index; member create/list/thread with `sender_phone`/`user_phone` joins, ownership 404 + RBAC 403 guards, admin queue/stats/status transitions IN_PROGRESS–RESOLVED–CLOSED with `resolution`, reopen-on-message, legacy `/api` alias; 38 checks) → 40 suites | QA Lead (pending) |
| 1.2 | 2026-09-09 | AI code review | Added test-currency (multi-currency/FX: identity/direct/inverse/triangulated rate resolution, public convert math, admin rate RBAC + guards, display currency persistence, ledgered TZS↔foreign convert with `CURRENCY_CONVERT` fx_rate/fx_base_currency evidence, round trip + insufficient funds; 41 checks) → 41 suites | QA Lead (pending) |
| 1.3 | 2026-09-09 | AI code review | Added test-qr (QR payments: STATIC/DYNAMIC create + ownership list, scan with payee `phone_number` join + scan_count + self-pay/expiry/unknown guards, pay via canonical transfer with balanced ledger journal + `qr_payments` row + QR meta txn, insufficient funds pre-guard, CSRF on anonymous DELETE, owner-only deactivate; fixed `u.phone`→`u.phone_number`/`u.name`→`u.full_name` scan join + `wallet_amount` NOT NULL on the QR txn insert; 33 checks) → 42 suites | QA Lead (pending) |
| 1.4 | 2026-09-09 | AI code review | Added test-insurance (micro-insurance: public product catalogue + category filter, purchase with canonical premium debit → balanced journal (DR CUSTOMER_WALLET = CR MNO_CLEARING) + `INSURANCE_PREMIUM` txn, product/age/insufficient-funds guards, ownership-scoped policies, renew advancing premium_paid + next_premium_date, failed renewal leaves policy intact; fixed `wallet_amount` NOT NULL on the premium + renewal txn inserts (was breaking purchase/renew end-to-end) + code-less 500s → `INSURANCE_*` 400/404 codes; 37 checks) → 43 suites | QA Lead (pending) |
| 1.5 | 2026-09-09 | AI code review | Added test-cards (virtual cards J1-J6: Luhn-valid PAN + masked number + CVV-once issuance with sha256(PAN)/CVV hashing, scheme prefixes + default/custom limits, ownership-scoped list/detail/statement with no PAN/CVV leak, limits update, freeze/unfreeze/block state machine, authorize → AUTH_HOLD via `lockWallet` with balanced CUSTOMER_WALLET/CARD_HOLD journal + decline-reason logging (CVV/per-txn/daily/insufficient), admin-only settlement (`captureLock` → MNO_CLEARING) + refunds (`unlockWallet`) both idempotent on auth_reference, statement + monthly summary; code-less 400/403/404 badge errors → `CARD_*` codes + `WALLET_INSUFFICIENT_FUNDS`; 71 checks) → 44 suites | QA Lead (pending) |
| 1.6 | 2026-09-10 | AI code review | Added test-saccos-foundation (SACCOS Digital Core increment 1, migration 101: org registration with unique code/name + config-driven JSONB setup + founder OWNER "0001" ACTIVE, compliance boundary row (regulatory_status TECH_INFRA, legal_entity), membership lifecycle invite-by-identity-phone with per-SACCOS member_number/accept/suspend/exit/re-invite-on-EXITED, GOV OWNER/BOARD/MEMBER RBAC (BOARD invites, OWNER-only activate/suspend), cross-entity isolation (non-member + cross-SACCOS reads 404, no enumeration), platform ADMIN oversight; routes env-gated `SACCOS_ENABLED=false` by default; 39 checks) → 45 suites | QA Lead (pending) |
| 1.7 | 2026-09-10 | AI code review | Added test-saccos-shares (SACCOS increment 2, migration 102: share subscription ledgered on the shared double-entry core — `debitWallet` DR CUSTOMER_WALLET / CR per-entity EQUITY `SACCOS<id>_SHARES_CAPITAL` + `financial_operations` claim + `SACCOS_SHARE_PURCHASE` txn + holdings base-cost motion; config-driven `shareStructure` {shareValue,minShares,maxShares,autoApprove}; PENDING/APPROVED/REJECTED lifecycle with OWNER/BOARD decisions; reject refunds via `creditWallet` on a fresh `-R` reference (DR equity / CR wallet) with txn marked `reversed_at`/`reversed_ref` (SUCCESS is terminal — no illegal status flip); summary totals; cross-entity isolation incl. per-entity equity codes; 38 checks) → 46 suites | QA Lead (pending) |
| 1.8 | 2026-09-10 | AI code review | Added test-saccos-savings (SACCOS increment 3, migration 103: member savings on the shared double-entry core — deposit `debitWallet` DR CUSTOMER_WALLET / CR per-entity LIABILITY `SACCOS<id>_SAVINGS_LIABILITY` (auto-upserted) + `SACCOS_SAVINGS_DEPOSIT` txn + movement; config-driven `savings` {savingsType,minDeposit,maxDeposit,minBalanceToRetain,autoApproveWithdrawals}; auto-approved withdrawal releases via `creditWallet` DR liability / CR wallet with `SACCOS_SAVINGS_WITHDRAWAL` txn + balance decrement; PENDING gated withdrawals reserve funds (SUM pending) and move nothing until OWNER/BOARD approve (`creditWallet` + movement APPROVED) — rejections are free (no reversal journal, SUCCESS terminal preserved); `SAV-<saccosId>-<memberNo>` account numbers, member RBAC 403, cross-entity isolation 404 incl. distinct liability codes, platform ADMIN oversight, audit trail; 38 checks) → 47 suites | QA Lead (pending) |
| 1.9 | 2026-09-10 | AI code review | Added test-saccos-credit (SACCOS increment 4, migration 104: loan applications SCL-* → OWNER/BOARD approval → disbursement on the shared double-entry core — `claimOperation` + `postJournal` DR per-entity ASSET `SACCOS<id>_LOANS_RECEIVABLE` / CR CUSTOMER_WALLET, `SACCOS_LOAN_DISBURSEMENT` txn; flat-interest `total_repayable = principal * (1 + rate% * months/12)` with proportional principal/interest repayment split — DR CUSTOMER_WALLET / CR LOANS_RECEIVABLE principal + CR `SACCOS<id>_INTEREST_INCOME` (REVENUE) — idempotent on REP-* refs, `WALLET_INSUFFICIENT_FUNDS` guard, close at zero outstanding (CLOSED + application REPAID); config-driven `lending` {interestRate,minAmount,maxAmount,maxTermMonths,maxActiveLoans,autoDisburse}; manual disburse + reject paths; member RBAC 403, cross-entity isolation 404 incl. distinct ASSET/REVENUE codes, platform ADMIN oversight, audit trail; 49 checks — caught + fixed a real summary bug (active_loans counted all loans, not only ACTIVE)) → 48 suites | QA Lead (pending) |
| 1.10 | 2026-09-10 | AI code review | Added test-saccos-governance (SACCOS increment 5, migration 105: entity-scoped collective decisioning on the SACCOS membership model — `saccos_resolutions` RES-* (DRAFT→OPEN→PASSED/REJECTED/CANCELLED) + `saccos_resolution_votes` (one vote FOR/AGAINST/ABSTAIN per member per resolution, enforced UNIQUE); OWNER/BOARD propose/open/close/cancel, ACTIVE members vote while OPEN (no money → no journal, every transition + vote audit-logged); closing tally: quorum = cast/ACTIVE members ≥ quorumPercent%, decision = FOR/cast ≥ decisionThresholdPercent% → PASSED else REJECTED; config `governance` {allowMemberVoting,quorumPercent,decisionThresholdPercent,votingDays} incl. officers-only voting mode (member vote 403); member RBAC 403, cross-entity isolation 404, platform ADMIN oversight on reads + summary, audit trail; 28 checks) → 49 suites | QA Lead (pending) |
| 1.11 | 2026-09-10 | AI code review | Added test-saccos-accounting (SACCOS increment 6, migration 106: entity-scoped bookkeeping + financial statements on the shared ledger — OWNER/BOARD open an accounting period PER-* (one OPEN at a time, SACCOS_ACC_PERIOD_ALREADY_OPEN), book internal journals ACC-* through it via `claimOperation` + `postJournal` against per-entity `SACCOS<id>_*` ledger accounts (EXPENSE: DR GENERAL_EXPENSE / CR OPERATING_CASH; INCOME: DR OPERATING_CASH / CR OTHER_INCOME), idempotent on reference (retry dedup), then close into immutable CLOSED with a statements snapshot (reopen CLOSED→OPEN is the only escape; booking into closed/no-period state → SACCOS_ACC_PERIOD_OPEN); statements computed from ledger_accounts + journal_entries scoped to the entity — chart (grouped ASSET/EXPENSE/REVENUE), trial balance (balanced true, DR=CR), income statement (revenue/expense/net), balance sheet (assets = liabilities + equity incl. net income) — members read, OWNER/BOARD write, cross-entity 404 (S2 books never touch S1 balances), platform ADMIN oversight, audit trail; 36 checks) → 50 suites | QA Lead (pending) |
| 1.12 | 2026-09-10 | AI code review | Added test-saccos-investments (SACCOS increment 7, FINAL, migration 107: entity-scoped member term investments — OWNER/BOARD define products INVP-* {minAmount, maxAmount, annualRatePercent, termMonths, ACTIVE/ARCHIVED}; members subscribe INV-* auto-approved or OWNER/BOARD-approved (config.investments.autoApprove) — subscription = `debitWallet` DR CUSTOMER_WALLET / CR per-entity LIABILITY `SACCOS<id>_INVESTMENTS_LIABILITY` + `SACCOS_INVESTMENT_SUBSCRIPTION` txn (insufficient funds rejected); redeems at maturity (flat interest = principal * rate% * term/12) via a 3-leg journal DR LIABILITY (principal) + DR `SACCOS<id>_INVESTMENT_INTEREST_EXPENSE` (EXPENSE) / CR CUSTOMER_WALLET, idempotent on RED-* with wallet FOR UPDATE; redeem before maturity → SACCOS_INV_NOT_MATURED, redeem CLOSED → SACCOS_INV_STATE, member-of-owner redeem → 403; reject path leaves PENDING→REJECTED with no funds moved; min/max guards SACCOS_INV_BELOW_MIN/ABOVE_MAX; cross-entity 404, ADMIN oversight summaries, audit trail; 32 checks — fixed a real maturity-date bug (pg DATE parsed as JS Date broke string comparison, letting an early redemption through)) → 51 suites | QA Lead (pending) |
| 1.22 | 2026-09-11 | AI code review | Added test-saccos-loan-mgmt (SACCOS increment 17, migration 117: guarantors/co-signers + restructure/reschedule + write-off into loan-loss reserves + standing orders — **guarantors**: borrower/OWNER/BOARD nominate an ACTIVE member GNT-* (PENDING→ACCEPTED→ACTIVE-on-disbursal), approval requires `lending.guaranteesRequired` ACCEPTED guarantees (SACCOS_LOAN_GUARANTOR_REQUIRED), each ACTIVE guarantor's backing limit raises the borrower's effective capacity (`guaranteed_cover` snapshot on application/loan); borrower removes a PENDING nomination (RELEASED), OWNER/BOARD charge an ACTIVE guarantee for OVERDUE installments in ascending order (`payGuarantorArrears` REPI-* through the installments core, guarantee → PAID_OUT with paid_amount, remaining installments pay out to CLOSED → guarantee RELEASED), borrower wallet untouched; **restructure**: OWNER/BOARD capitalise an ACTIVE balance at a new flat rate/term (RST-*, schedule rebuilt from the paid point onward, `schedule_version` bumps, receivable principal-delta journaled CR/DR `SACCOS<id>_LOANS_RECEIVABLE` vs `SACCOS<id>_LOAN_LOSS_EXPENSE`) with term/rate guards; **write-off**: extinguish an ACTIVE balance (reason required) against the utilised LLR provision first then the un-provisioned principal shortfall as expense — DR `SACCOS<id>_LOAN_LOSS_RESERVES` (min(provisioned, remaining principal)) + DR `SACCOS<id>_LOAN_LOSS_EXPENSE` / CR `SACCOS<id>_LOANS_RECEIVABLE` (remaining principal; interest forgiven tracked not journaled) on WO-* (SACCOS_LOAN_WRITE_OFF_REASON/STATE, member 403), loan+application → WRITTEN_OFF, remaining installments CANCELLED (CHECK widened), LLR row UTILIZED, guarantees released, repay → SACCOS_LOAN_WRITTEN_OFF; **standing orders**: monthly recurring contributions (SO-*, day_of_month 1–28 CHECK, SACCOS_STANDING_ORDER_TYPE/AMOUNT/DAY/TARGET/STATE, RBAC 403 on manual run) into savings / an ACTIVE fund / an ACTIVE welfare scheme (auto-joins) / an ACTIVE own loan — inner flows keep their own transaction, due = ACTIVE and next_run_at NULL/≤ today, success schedules next month, failure increments fail_runs + persists last_error and DEACTIVATES after 3 consecutive; isolation (outsider 404, cross-saccos fund target 400 SACCOS_STANDING_ORDER_TARGET, cross-org guarantee 404) + 6 audit actions; 110 checks, green twice — development fixed three real bugs: PG `CASE WHEN $2 <= 0` integer inference crashing on decimal repayments (`::numeric` casts), stale `amount_outstanding` across consecutive payments in a loop (SQL-side `-= $1::numeric ... RETURNING`), write-off CR'ing full outstanding vs principal DR'd at disbursal (receivable would drift negative — CR remaining principal), plus JS float 2dp rounding in wallet/outstanding assertions) → 61 suites | QA Lead (pending) |
| 1.21 | 2026-09-11 | AI code review | Added test-saccos-backing-welfare (SACCOS increment 16, migration 116: savings-backed loan limits + member welfare/social fund - collateral snapshot columns on `saccos_loan_applications` (backing_enabled/backing_multiple/backing_balance/backing_limit); when `saccos.config.lending.savingsBackingEnabled` (default OFF) backing_balance = member savings account balance + share-holdings book value and backing_limit = balance x `savingsBackingMultiple` (default 3); `POST loans/apply` rejects requested_amount > backing_limit (SACCOS_LOAN_BACKING_INSUFFICIENT) and snapshots the values; `loans/applications/:id/approve` re-checks LIVE backing so savings withdrawn after applying voids approval (app stays PENDING) and refreshes the snapshot on success; member-facing `GET loans/backing` exposes savings_balance/share_value/backing_balance/backing_limit; welfare runs over the shared ledger with fund account `SACCOS<id>_WELFARE_FUND` (LIABILITY, auto-upserted): `saccos_welfare_schemes` (name/contribution/payout/ACTIVE|ARCHIVED, UNIQUE(saccos_id,name) SACCOS_WELFARE_SCHEME_EXISTS, OWNER/BOARD create, member 403 SACCOS_RBAC, cross-entity 403 SACCOS_NOT_MEMBER) + `saccos_welfare_contributions` (WLC-*, UNIQUE(scheme_id,member_id) join-once SACCOS_WELFARE_ALREADY_JOINED; amount >= contribution SACCOS_WELFARE_CONTRIBUTION_LOW, wallet pre-check WALLET_INSUFFICIENT_FUNDS) ledgered via `debitWallet` DR CUSTOMER_WALLET / CR fund + `saccos_welfare_claims` (WLF-* SUBMITTED->APPROVED/REJECTED->PAID, capped by scheme payout SACCOS_WELFARE_CLAIM_AMOUNT, one active claim per member-scheme SACCOS_WELFARE_CLAIM_ACTIVE, review only from SUBMITTED SACCOS_WELFARE_CLAIM_STATE); payout pays an APPROVED claim via `creditWallet` DR fund / CR CUSTOMER_WALLET on the idempotent WLF-* ref with a fund-balance pre-check (contributions - payouts, never negative) SACCOS_WELFARE_FUND_INSUFFICIENT; governing summary closes the kitty (fund_balance/total_contributions/pending/paid), members see own schemes/contributions/claims; SACCOS_WELFARE_SCHEME_CREATED/CONTRIBUTION/CLAIM_PAID audit; dashboard loans card shows live backing capacity + new welfare card (schemes/join/claim + approve/reject/pay); 60 checks - backing off-by-default, 50000-savings x3 = 150000 cap (apply 200000 rejected), snapshot at apply, withdrawal-defeated approval, restore + approve refreshes snapshot + auto-disburses, savings+shares backing (10000 + 20000 = 90000), contribution DR/CR journal + exact wallet debit + fund balance, join-once + low + insufficient guards, claim lifecycle with fund-insufficient-before-third-contribution and 50000 payout out of a 60000 kitty (fund 10000), idempotent WLF-* pay, cross-entity isolation + audit) -> 60 suites | QA Lead (pending) |
| 1.20 | 2026-09-11 | AI code review | Added test-saccos-savings-interest (SACCOS increment 15, migration 115: savings interest accrual & posting - periodic interest on member savings closing the savings<->credit economic loop: one PENDING/POSTED cycle per calendar month (`saccos_savings_interest_cycles`, period-unique UNIQUE(saccos_id, period)), rate from `saccos.config.savings.interestRatePercent` (annual %, SACCOS_SAVINGS_INTEREST_RATE if unset); OWNER/BOARD `POST /:id/savings-interest/prepare` snapshots ACTIVE members with a positive balance onto the current month into `saccos_savings_interest_awards` (UNIQUE(cycle_id, member_id)) at monthly = ROUND(balance x rate/100/12, 2) with total_interest = SUM of rounded awards so member balances reconcile exactly to the liability; re-preparing the same month recomputes idempotently (posted month -> SACCOS_SAVINGS_INTEREST_CYCLE_EXISTS); `POST cycles/:cycleId/post` posts a balanced journal DR `SACCOS<id>_SAVINGS_INTEREST_EXPENSE` (EXPENSE) / CR `SACCOS<id>_SAVINGS_LIABILITY` (LIABILITY), claims an SVI-* op (re-post -> SACCOS_SAVINGS_INTEREST_STATE), credits each member's account (balance + INTEREST/APPROVED movement, award POSTED with txn_reference) and flips the cycle POSTED with posted_at/posted_by; no awards -> SACCOS_SAVINGS_INTEREST_NO_AWARDS; migration 115 also widens the savings-movements CHECK to allow INTEREST; members read own history (`mine`), governing summary/cycle list/detail roster with member_number + user full_name; RBAC 403, cross-entity 404, platform ADMIN-no-membership 403 SACCOS_NOT_MEMBER, SACCOS_SAVINGS_INTEREST_PREPARE/POST audit; 30 checks - balance 50000@6%->250 + 30000@6%->150 (total 400), journal DR=CR, member wallet credited + statement reflects post, no-duplicate idempotency, next-month empty cycle NO_AWARDS, isolation guards) -> 59 suites | QA Lead (pending) |
| 1.19 | 2026-09-11 | AI code review | Added test-saccos-meetings (SACCOS increment 14, migration 114: digital meetings, attendance & minutes - a meeting lifecycle layered over governance: DRAFT→OPEN→CLOSED→MINUTES_PUBLISHED with structured, positioned agenda items (`saccos_meeting_agenda_items`) and idempotent per-member attendance (`saccos_meeting_attendance`, UNIQUE(meeting_id, member_id)): ACTIVE members self check-in (PRESENT) while OPEN, OWNER/BOARD may mark anyone PRESENT/ABSENT/EXCUSED (upsert), closing records quorum_met = (PRESENT+EXCUSED) vs ACTIVE members × meeting `quorum_pct` (default 50, ceiling), minutes published only from CLOSED with non-empty text + marked-complete agenda items and a published_at timestamp; OWNER/BOARD create/open/close/publish, all ACTIVE members list (present_count + my_status) and view detail (roster with member_number + user full_name); validation SACCOS_MEETING_TITLE/DATES/QUORUM/STATE/MINUTES_REQUIRED/ATTENDANCE_STATE + RBAC 403 + cross-entity 404 + platform ADMIN-no-membership 403 SACCOS_NOT_MEMBER; 5 SACCOS_MEETING_* audit actions; 39 checks - quorum math (3 ACTIVE @50% need 2, present 3 → met; empty meeting → not met), idempotent duplicate check-in, self-check-in overriding EXCUSED, agenda completion fidelity) → 58 suites | QA Lead (pending) |
| 1.18 | 2026-09-11 | AI code review | Added test-saccos-arrears (SACCOS increment 13, migration 113: overdue arrears & late-fee accrual - completes the OVERDUE lifecycle introduced in 112). PENDING installments past the grace window (`saccos.config.lending.graceDays`, default 0) are flipped OVERDUE on read or by board `POST /:id/loans/recompute-arrears` with `days_late` + late fee `total * lateFeePercent% * ceil(days/30)` accrued deterministically and idempotently per day; paying an OVERDUE installment charges total + late_fee (`SACCOS<id>_LATE_FEE_INCOME` REVENUE leg auto-upserted, DR wallet / CR receivable + interest + late fee, balanced) while `amount_outstanding` decrements ONLY by the installment total (the fee is income); owed total is wallet-gated `WALLET_INSUFFICIENT_FUNDS` on the full charge; governing `GET /:id/loans/arrears` snapshot (fresh mark-overdue on read) + member list summary exposes overdue count/fees; 27 checks - grace keeps a recent miss PENDING (no fee), read-path vs recompute-path equivalence, ledger evidence (LATE_FEE_INCOME 244.8, INTEREST_INCOME 240, receivable 0), wallet accounting (200000 + 12000 - 6364.8 - 6120), RBAC (member 403 SACCOS_RBAC, cross-entity/platform-ADMIN 403 SACCOS_NOT_MEMBER), SACCOS_ARREARS_RECOMPUTE audit - development also fixed read-before-write in the installment pay path (`fetchInstallment` must read inside the txn client) and PostgreSQL `jsonb_set` cannot create two missing nested levels (merge-`||` used in the harness) → 57 suites | QA Lead (pending) |
| 1.17 | 2026-09-11 | AI code review | Added test-saccos-installments (SACCOS increment 12, migration 112: loan installments + interest accrual — a per-loan amortisation schedule layered over the credit module: term_months rows of principal_part + interest_part summing exactly to principal and total_repayable−principal (last row absorbs rounding) generated deterministically and idempotently, materialised lazily on first read or explicitly by OWNER/BOARD via `POST :/loans/:loanId/installments/generate` (regenerate → SACCOS_LOAN_INSTALLMENTS_EXIST); membership-locked (own loan only, sibling 403, cross-entity 404, scheme ADMIN 403 without membership); each payment claims a REPI-* op + journals 3 legs DR CUSTOMER_WALLET / CR `SACCOS<id>_LOANS_RECEIVABLE` (principal_part) + CR `SACCOS<id>_INTEREST_INCOME` (interest_part) with wallet FOR UPDATE, `amount_outstanding -= total`, loan CLOSED + application REPAID at zero, SACCOS_LOAN_INSTALLMENT_PAYMENT txn + repayments row; strict ordering (paying a later one → SACCOS_LOAN_INSTALLMENT_ORDER; duplicate → SACCOS_LOAN_INSTALLMENT_ALREADY_PAID); governing summary ENDPOINT `GET /:id/loans/installments/summary` (member 403); ledger evidence (interest income recognised 900 CR−DR as revenue, receivable back to 0) + audit trail; 32 checks — fixed id mixing while wiring payments (`membership.id` is the saccos_members row, not users.id: wallet/txn must use user_id, repayments/installments member FK is saccos_members.id)) → 56 suites | QA Lead (pending) |
| 1.16 | 2026-09-11 | AI code review | Added test-saccos-reports (SACCOS increment 11, migration 111: member statements + regulatory/management reporting - consolidated per-member statement (savings/shares/loans/investments/dividends/exits/wallet) self-readable incl. EXITED members, sibling reads OWNER/BOARD-only (SACCOS_RBAC), non-member 404; regulatory snapshot computed from the shared double-entry ledger (per-entity `SACCOS<id>_*` codes totaled by account type - LIABILITY/EQUITY/REVENUE hold credit, ASSET/EXPENSE debit) + saccos tables, UPSERTed into `saccos_regulatory_reports` keyed by (saccos_id, as_of) - dated immutable picture with members/shares/liabilities/loans+LLR/dividends/revenue/expense/net-income/exits; monthly management flows (deposits, withdrawals, disbursements, repayments, dividends, fund contributions, exit settlements) over N=6 default/24 max months; ADMIN oversight, SACCOS_REGULATORY_REPORT + SACCOS_MANAGEMENT_REPORT audit; 28 checks - caught a real increment-10 bug: exit settlement paid a leaver the whole entity SAVINGS_LIABILITY instead of their own deposit balance, fixed to settle the member's own savings account book value) 55 suites | QA Lead (pending) |
| 1.15 | 2026-09-11 | AI code review | Added test-saccos-exits (SACCOS increment 10, migration 110: member exit & settlement closing the membership lifecycle - ACTIVE member (never OWNER, SACCOS_RBAC; OWNER exits stay open) settles the book value atomically via `settleAndExit`: savings `creditWallet` DR per-entity LIABILITY `SACCOS<id>_SAVINGS_LIABILITY` / CR wallet on `SXC-*-:SAV` refs; shares redeemed at holdings book value with a balanced 3-step (claim `SXC-*-:SHR` + postJournal DR EQUITY `SACCOS<id>_SHARES_CAPITAL` / CR CUSTOMER_WALLET + wallet FOR UPDATE + `auditBalance` + holdings zeroed); every PENDING dividend payout on DECLARED runs is paid on the payout's own `DIVP-*` reference (DR EXPENSE `SACCOS<id>_DIVIDEND_DISTRIBUTED` / CR wallet, marked PAID) so a later board distribution settles only remaining members without double-paying leavers; single settlement per member (UNIQUE member_id, SACCOS_EXIT_ALREADY_SETTLED), membership flips EXITED then re-settle -> SACCOS_MEMBER_STATUS_INVALID; exit row records savings_settled/share_count_settled/share_redemption_amount/dividend_settled/total_settlement (SXC-*); self-only 403, cross-entity 404, ADMIN lists + member mine, SACCOS_EXIT_SETTLED audit; 23 checks - fixed dividend account lazy-creation (paid an undeclared EXPENSE code) + share redemption not mutating `users.wallet_balance`) 54 suites | QA Lead (pending) |
| 1.14 | 2026-09-11 | AI code review | Added test-saccos-funds (SACCOS increment 9, migration 109: fund management + loan-loss reserves on the shared ledger — OWNER/BOARD create per-SACCOS LIABILITY buckets `SACCOS<id>_FUND_<CODE>` (FND-*, code-regex SACCOS_FUND_CODE, UNIQUE(saccos_id,code) SACCOS_FUND_EXISTS, target/minimum balances) with the ledger account auto-upserted; ACTIVE members contribute via `debitWallet` DR CUSTOMER_WALLET / CR fund account + `SACCOS_FUND_CONTRIBUTION` txn with fund meta (FNC-*), pre-checked WALLET_INSUFFICIENT_FUNDS; OWNER/BOARD transfer between funds (balanced journal DR from / CR to, FTF-*) with SACCOS_FUND_SAME / SACCOS_FUND_INSUFFICIENT guards; archive only at zero residual (SACCOS_FUND_HAS_BALANCE); members see own contributions (mine), ADMIN oversight summaries, cross-entity 404; loan-loss reserves provision a disbursed loan once (UNIQUE(saccos_id,loan_id) SACCOS_LLR_EXISTS; unknown loan 404) — DR `SACCOS<id>_LOAN_LOSS_EXPENSE` / CR `SACCOS<id>_LOAN_LOSS_RESERVES`, LLR-*, release reverses and re-release -> SACCOS_LLR_STATE; P&L reflects provisions immediately (ledger-computed: 30000 while both provisioned, 20000 after release); 47 checks — fixed a debit-sign bug in fund balance projection (LIABILITY accounts hold credit) + normalized provision rate decimals) → 53 suites | QA Lead (pending) |
| 1.13 | 2026-09-10 | AI code review | Added test-saccos-dividends (SACCOS increment 8, migration 108: surplus distribution closing the economic loop — OWNER/BOARD declare a dividend run DIV-* against a CLOSED accounting period (the authoritative surplus source, one run per period → SACCOS_DIV_PERIOD_USED; OPEN → SACCOS_DIV_PERIOD_CLOSED), per-share derived from totalAmount / eligible shares or given directly (SACCOS_DIV_PER_SHARE/TOTAL); eligible = ACTIVE members' holdings (SACCOS_DIV_NO_SHARES); declaration snapshots per-holder payouts DIVP-* (share_count × per_share, PENDING); distribution pays each holder via `claimOperation` + `postJournal` DR per-entity EXPENSE `SACCOS<id>_DIVIDEND_DISTRIBUTED` / CR CUSTOMER_WALLET with wallet FOR UPDATE + `auditBalance`, idempotent per DIVP-* (re-distribute pays nothing extra); run → DISTRIBUTED only when no PENDING payouts remain (partial retry stays DECLARED), summary paid/paid_count/pending_count; members read own payouts (mine), lists + summaries on ADMIN oversight, member RBAC 403, cross-entity 404, distinct per-entity expense codes, audit trail; 23 checks — fixed a `$8`-parameter-less INSERT bug and a non-existent `gen_random_bytes` function (replaced with md5-encoded refs) during first green) → 52 suites | QA Lead (pending) |